/**
 * `server-only` lança ao ser importado fora do runtime de servidor do Next.
 * Nos testes a camada de domínio roda direto no Node — este stub neutraliza
 * o guarda sem afastar o código de produção da convenção do projeto.
 */
export {};
